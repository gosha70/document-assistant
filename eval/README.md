# Evaluation Directory

This directory contains evaluation datasets, harnesses, and baseline results for the Document Assistant RAG pipeline.

## Structure

- `baseline_dataset.json` — labeled question/answer/context triples for regression testing
- `baseline_results.json` — baseline retrieval and generation quality metrics (Phase 1)
- `rubrics/` — LLM-as-judge rubrics for answer quality evaluation
